import "./header.css"

const Header = () => {
    return (
        <header className="header">
            <h2 className="header-title">Page title</h2>
        </header>
    )
};

export default Header;